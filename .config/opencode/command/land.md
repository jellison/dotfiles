---
description: Land the current feature branch onto the default branch (squash, rebase, verify, cherry-pick, clean up)
---

Use the `land` skill to land the current feature branch onto the repository's default branch. Follow the skill's workflow exactly: squash the branch's commits, rebase onto the default branch, verify the build and tests, cherry-pick onto the default branch, and delete the source branch/worktree. Never push.
